thredds:
  build: .
  ports:
   - "8801:8080"
  volumes:
   - /disk/data/thredds/webapps:/usr/local/tomcat/webapps
   - /disk/data/thredds/content:/usr/local/tomcat/content/thredds
   - /disk/data/carbontracker:/usr/local/tomcat/content/thredds/public/carbontracker
