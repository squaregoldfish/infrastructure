# This ansible playbook will provision the STILT servers.

---
- import_playbook: setup.yml
  
- hosts: stiltmaster
  become: true
  vars_files: [vars.yml, vault.yml]
  roles: [icos.stiltweb]

- hosts: stiltcluster
  become: true
  vars_files: [vars.yml, vault.yml]
  roles: [icos.stiltrun, icos.stiltcluster]
