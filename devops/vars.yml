#Set resore to true only if you want to restore from backup.
restore               : false
#Base path for backup directory. Only used if backup is set to true, else ignore
backup_path           : /disk/ServerBackup/Backup/
doi_password          : "{{ vault_doi_password }}"
cpauth_oauth_conf     : "{{ vault_cpauth_oauth_conf }}"
cpauth_mailing_pass   : "{{ vault_cpauth_mailing_pass }}"
