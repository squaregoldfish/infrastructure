- name: Install docker and pip3
  apt:
    name:
      - docker.io
      - python3-pip
    state: present
    cache_valid_time: 3600

- name: Install docker-compose
  pip:
    name:
      - docker-compose
    state: present
    executable: pip3
