- include_tasks: drupal_only.yml
  with_items: "{{ websites }}"
  loop_control:
    loop_var: project_name
  tags:
    - drupal_only

- name: Setup borg backup
  import_tasks: backup.yml
  when: skip_borg_backup is not defined
