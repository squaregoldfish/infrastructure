# We store the tasks in another file just so we can add a tag when including
# them. While it might have be easier to add the tag at the point of
# 'include_role', that might have tainted this roles' dependencies with a tag
# meant only for this role.
- import_tasks: deploy.yml
  tags: deploy
