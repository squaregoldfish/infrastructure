- import_tasks: setup.yml
  tags: climbeco_setup

- import_tasks: copy.yml
  tags:
    - climbeco_copy
    - climbeco_deploy

- name: Build and start the hub image
  tags: climbeco_deploy
  docker_compose:
    project_src: "{{ climbeco_home }}"
    build: yes
