rdflogdb:
  image: postgres:9.4.4
  ports:
   - "5433:5432"
  environment:
   - POSTGRES_PASSWORD=password
   - PGDATA=/var/lib/postgresql/data/pgdata
  volumes:
   - /disk/data/postgres/rdflog:/var/lib/postgresql/data

